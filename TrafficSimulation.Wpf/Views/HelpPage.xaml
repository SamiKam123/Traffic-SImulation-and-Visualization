<UserControl x:Class="TrafficSimulation.Wpf.Views.HelpPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Spacing="16" Margin="0,0,24,0">
            <TextBlock Text="Getting started" FontSize="20" FontWeight="SemiBold" />
            <TextBlock TextWrapping="Wrap"
                       Text="Load a preset or configure a new intersection in the Setup page. When ready, run the simulation to visualise throughput, queues and pedestrian crossings." />
            <Separator />
            <TextBlock Text="Tips" FontSize="20" FontWeight="SemiBold" />
            <ItemsControl>
                <ItemsControl.Items>
                    <TextBlock Text="• Zoom with your mouse wheel and pan with the middle button." />
                    <TextBlock Text="• Toggle overlays for signal states, queues and delays." />
                    <TextBlock Text="• Export frame captures directly from the visualization canvas." />
                </ItemsControl.Items>
            </ItemsControl>
        </StackPanel>
        <Border Grid.Column="1" Background="{StaticResource PrimaryBrush20}" CornerRadius="12" Padding="24">
            <StackPanel Spacing="12">
                <TextBlock Text="Need more?" FontWeight="SemiBold" />
                <TextBlock TextWrapping="Wrap"
                           Text="Visit the documentation site or reach out to the maintainers for workflow-specific guidance." />
                <Button Content="Open docs" Width="140" HorizontalAlignment="Left" />
                <Button Content="Contact support" Width="160" HorizontalAlignment="Left" Background="{StaticResource PrimaryBrush80}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
