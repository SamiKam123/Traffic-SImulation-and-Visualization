<UserControl x:Class="TrafficSimulation.Wpf.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Spacing="24">
            <GroupBox Header="Intersection Layout">
                <UniformGrid Columns="2" Margin="12" HorizontalAlignment="Stretch">
                    <StackPanel Margin="12">
                        <TextBlock Text="Number of lanes" />
                        <Slider Minimum="1" Maximum="6" Value="3" TickFrequency="1" IsSnapToTickEnabled="True" />
                    </StackPanel>
                    <StackPanel Margin="12">
                        <TextBlock Text="Lane width (m)" />
                        <Slider Minimum="2.5" Maximum="5" Value="3.5" TickFrequency="0.5" />
                    </StackPanel>
                    <StackPanel Margin="12">
                        <TextBlock Text="Turn ratio" />
                        <ComboBox SelectedIndex="1">
                            <ComboBoxItem Content="Balanced" />
                            <ComboBoxItem Content="Left-heavy" />
                            <ComboBoxItem Content="Right-heavy" />
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Margin="12">
                        <TextBlock Text="Pedestrian phase" />
                        <CheckBox Content="Enable scramble" />
                        <CheckBox Content="Accessible signals" IsChecked="True" />
                    </StackPanel>
                </UniformGrid>
            </GroupBox>
            <GroupBox Header="Vehicle Behaviour">
                <StackPanel Margin="12" Spacing="12">
                    <CheckBox Content="Enable autonomous agents" IsChecked="True" />
                    <CheckBox Content="Allow lane changes" IsChecked="True" />
                    <StackPanel>
                        <TextBlock Text="Acceleration profile" />
                        <ComboBox SelectedIndex="0">
                            <ComboBoxItem Content="Normal" />
                            <ComboBoxItem Content="Aggressive" />
                            <ComboBoxItem Content="Conservative" />
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
            <GroupBox Header="Export">
                <StackPanel Margin="12" Spacing="12">
                    <Button Content="Save configuration" Width="180" />
                    <TextBlock TextWrapping="Wrap" Foreground="{StaticResource SecondaryForegroundBrush}"
                               Text="Save presets so you can quickly load and reuse complex intersection designs." />
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
